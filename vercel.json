{
  "installCommand": "git clone https://github.com/flutter/flutter.git --depth 1 -b 3.44.0 $HOME/flutter && export PATH=\"$HOME/flutter/bin:$PATH\" && flutter config --enable-web && flutter pub get",
  "buildCommand": "export PATH=\"$HOME/flutter/bin:$PATH\" && flutter build web --release --base-href /",
  "outputDirectory": "build/web"
}
